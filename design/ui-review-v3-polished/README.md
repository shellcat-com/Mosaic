# Mosaic v3 polished UI review

Captured from the real focused SwiftUI target on iPhone 17 Pro / iOS 26.3 on 2026-08-25.

The Debug-only `MOSAIC_SHOWCASE_SCREEN` fixture supplies deterministic signed-in data without changing production authentication, networking, or hosted data. Bundled public-domain artwork is used as stand-in photo content in the debug gallery and camera only; production photos come exclusively from developed in-app camera JPEGs.

- `01-home.png`: compact library with active ceramic side and completed artwork
- `create.png`: guided six-step creation foundation
- `active.png`: sealed artwork promise, ceramic kindness side, and fixed countdown
- `revealed.png`: completed artwork destination after reveal
- `photos.png`: separate disposable-photo gallery
- `recap.png`: photo-only template and licensed-music styling
- `camera.png`: event-scoped disposable camera, exposure counter, shutter, and sealed roll
- `accessibility-home.png`: Accessibility XXXL adaptive layout evidence

The active event deliberately uses a sealed artwork face. The artwork itself is not previewed in invitations, active-event cards, or the kindness-side explanation.
